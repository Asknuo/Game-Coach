line-length = 120
target-version = "py310"

[lint]
# CI 门禁规则集：正确性优先（F=pyflakes, E4/E7/E9=pycodestyle 关键项,
# I=import 排序, UP=现代化）。风格类大改写不开禁。
select = ["E4", "E7", "E9", "F", "I", "UP"]
ignore = [
    "E741",  # 单字母变量 l（数学公式场景多）
]
